package erp.payroll.model;

import java.util.Date;

/**
 * [급여관리] 월별 급여계산 회차 마스터 Model DB 테이블: PAYROLL_RUN
 */
// 급여급여 회차 정보를 보관하고 관련 기능에서 사용할 수 있도록 제공한다.
// 給与給与回次情報を保持し、関連機能から利用できるように提供する。
public class PayrollRun {

	// [DB 관리 항목]
	// データベースの主キー・外部キー・基本属性を保持し、永続化対象を識別する。
	private int payrollRunId; // 급여계산 회차 식별 번호 (PK)

	// [귀속연월과 급여차수]
	// 基準日と期間の境界を確認し、照会・計算に使用できる日付形式へ変換する。
	private String payYear; // 귀속연월 (연도, YYYY)
	private String payMonth; // 귀속연월 (월, MM)
	private String paySeq; // 급여차수 (01~10)

	// [급여회차 유형]
	// 支給額・控除額・期間値を業務基準に従って計算し、合計と最終金額へ反映する。
	private String incomeType; // 자동 저장 항목: 급여 화면 구분 (0=일반소득, 1=사업/기타소득, 2=일용직)

	// [정산기간·급여지급일]
	// 基準日と期間の境界を確認し、照会・計算に使用できる日付形式へ変換する。
	private Date calcStartDate; // 정산기간 (시작일)
	private Date calcEndDate; // 정산기간 (종료일)
	private Date payDate; // 급여지급일

	// 전달받은 값으로 급여급여 회차 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で給与給与回次オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public PayrollRun() {
	}

	// Getter & Setter
	// 급여급여 회차 객체에 저장된 급여급여 회차식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された給与給与回次識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getPayrollRunId() {
		return payrollRunId;
	}

	// 전달받은 급여급여 회차식별번호 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った給与給与回次識別番号の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPayrollRunId(int payrollRunId) {
		this.payrollRunId = payrollRunId;
	}

	// 급여급여 회차 객체에 저장된 지급연도 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された支給年度の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getPayYear() {
		return payYear;
	}

	// 전달받은 지급연도 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った支給年度の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPayYear(String payYear) {
		this.payYear = payYear;
	}

	// 급여급여 회차 객체에 저장된 지급월 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された支給月の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getPayMonth() {
		return payMonth;
	}

	// 전달받은 지급월 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った支給月の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPayMonth(String payMonth) {
		this.payMonth = payMonth;
	}

	// 급여급여 회차 객체에 저장된 지급Seq 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された支給Seqの値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getPaySeq() {
		return paySeq;
	}

	// 전달받은 지급Seq 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った支給Seqの値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPaySeq(String paySeq) {
		this.paySeq = paySeq;
	}

	// 급여급여 회차 객체에 저장된 소득구분 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された所得区分の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getIncomeType() {
		return incomeType;
	}

	// 전달받은 소득구분 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った所得区分の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setIncomeType(String incomeType) {
		this.incomeType = incomeType;
	}

	// 급여급여 회차 객체에 저장된 계산Start일자 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された計算Start日付の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Date getCalcStartDate() {
		return calcStartDate;
	}

	// 전달받은 계산Start일자 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った計算Start日付の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setCalcStartDate(Date calcStartDate) {
		this.calcStartDate = calcStartDate;
	}

	// 급여급여 회차 객체에 저장된 계산End일자 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された計算End日付の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Date getCalcEndDate() {
		return calcEndDate;
	}

	// 전달받은 계산End일자 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った計算End日付の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setCalcEndDate(Date calcEndDate) {
		this.calcEndDate = calcEndDate;
	}

	// 급여급여 회차 객체에 저장된 지급일자 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 給与給与回次オブジェクトに保存された支給日付の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Date getPayDate() {
		return payDate;
	}

	// 전달받은 지급일자 값을 급여급여 회차 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った支給日付の値を給与給与回次オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPayDate(Date payDate) {
		this.payDate = payDate;
	}
}
