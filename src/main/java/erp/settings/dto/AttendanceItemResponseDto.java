package erp.settings.dto;

/**
 * [기본환경설정] 근태항목 상세 정보 및 그룹명·휴가공제명 포함 응답 DTO 근태 설정 화면 관리 그리드용
 */
// 근태항목응답정보 처리에 필요한 값을 계층 간에 전달한다.
// 勤怠項目レスポンス情報処理に必要な値を各階層間で受け渡す。
public class AttendanceItemResponseDto {

	// [ATTENDANCE_ITEM 기본 정보]
	// データベースの主キー・外部キー・基本属性を保持し、永続化対象を識別する。
	private int attendanceItemId; // 근태항목 식별 번호 (PK)
	private int attendanceGroupId; // 근태그룹 외래키
	private String attendName; // 근태항목명
	private String unitType; // 단위구분
	// 근태항목응답정보 객체에 저장된 식별번호 값을 반환한다.
	// Controller·Service·DAO·JSP 사이에서 동일한 데이터를 일관된 구조로 전달하기 위해 사용한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された識別番号の値を返す。
	// Controller・Service・DAO・JSP間で同じデータを一貫した構造として受け渡すために使用する。
	private Integer deductLeaveId; // 차감 연동 휴가항목 ID (외래키)
	private String workHourType; // 근로시간연계 유형
	private String useYn; // 사용여부 (Y/N)

	// [조인(Join)을 통해 가져오는 연관 명칭 데이터]
	// 関連テーブルを識別キーで結合し、画面表示に必要な名称と詳細情報をまとめて取得する。
	private String groupName; // 소속된 근태그룹명 (예: 사무직군, 현장직군)
	private String deductLeaveName; // 차감 연동된 휴가항목명 (예: 연차, 보건휴가)

	// 전달받은 값으로 근태항목응답정보 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で勤怠項目レスポンス情報オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public AttendanceItemResponseDto() {
	}

	// Getter & Setter
	// 근태항목응답정보 객체에 저장된 근태항목식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された勤怠項目識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getAttendanceItemId() {
		return attendanceItemId;
	}

	// 전달받은 근태항목식별번호 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った勤怠項目識別番号の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setAttendanceItemId(int attendanceItemId) {
		this.attendanceItemId = attendanceItemId;
	}

	// 근태항목응답정보 객체에 저장된 근태그룹식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された勤怠グループ識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getAttendanceGroupId() {
		return attendanceGroupId;
	}

	// 전달받은 근태그룹식별번호 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った勤怠グループ識別番号の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setAttendanceGroupId(int attendanceGroupId) {
		this.attendanceGroupId = attendanceGroupId;
	}

	// 근태항목응답정보 객체에 저장된 Attend명칭 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存されたAttend名称の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getAttendName() {
		return attendName;
	}

	// 전달받은 Attend명칭 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったAttend名称の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setAttendName(String attendName) {
		this.attendName = attendName;
	}

	// 근태항목응답정보 객체에 저장된 단위구분 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された単位区分の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getUnitType() {
		return unitType;
	}

	// 전달받은 단위구분 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った単位区分の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setUnitType(String unitType) {
		this.unitType = unitType;
	}

	// 근태항목응답정보 객체에 저장된 공제휴가식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された控除休暇識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Integer getDeductLeaveId() {
		return deductLeaveId;
	}

	// 전달받은 공제휴가식별번호 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った控除休暇識別番号の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setDeductLeaveId(Integer deductLeaveId) {
		this.deductLeaveId = deductLeaveId;
	}

	// 근태항목응답정보 객체에 저장된 근무Hour구분 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された勤務Hour区分の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getWorkHourType() {
		return workHourType;
	}

	// 전달받은 근무Hour구분 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った勤務Hour区分の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setWorkHourType(String workHourType) {
		this.workHourType = workHourType;
	}

	// 근태항목응답정보 객체에 저장된 사용여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された使用可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getUseYn() {
		return useYn;
	}

	// 전달받은 사용여부 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った使用可否の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setUseYn(String useYn) {
		this.useYn = useYn;
	}

	// 근태항목응답정보 객체에 저장된 그룹명칭 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存されたグループ名称の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getGroupName() {
		return groupName;
	}

	// 전달받은 그룹명칭 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったグループ名称の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setGroupName(String groupName) {
		this.groupName = groupName;
	}

	// 근태항목응답정보 객체에 저장된 공제휴가명칭 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 勤怠項目レスポンス情報オブジェクトに保存された控除休暇名称の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getDeductLeaveName() {
		return deductLeaveName;
	}

	// 전달받은 공제휴가명칭 값을 근태항목응답정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った控除休暇名称の値を勤怠項目レスポンス情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setDeductLeaveName(String deductLeaveName) {
		this.deductLeaveName = deductLeaveName;
	}
}
