package erp.attendance.service.request;


//일용직 근무 월별 조회에서 사용
// 일용직근무목록요청정보 처리에 필요한 값을 계층 간에 전달한다.
// 日雇い勤務一覧リクエスト情報処理に必要な値を各階層間で受け渡す。
public class DailyWorkListRequest {
	private int year;
	private int month;
	private Integer deptId;
	private Integer posId;

	// 전달받은 값으로 일용직근무목록요청정보 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で日雇い勤務一覧リクエスト情報オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public DailyWorkListRequest() {
	}

	// 전달받은 값으로 일용직근무목록요청정보 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で日雇い勤務一覧リクエスト情報オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public DailyWorkListRequest(int year, int month, Integer deptId, Integer posId) {
		super();
		this.year = year;
		this.month = month;
		this.deptId = deptId;
		this.posId = posId;
	}

	// 일용직근무목록요청정보 객체에 저장된 연도 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 日雇い勤務一覧リクエスト情報オブジェクトに保存された年度の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getYear() {
		return year;
	}

	// 전달받은 연도 값을 일용직근무목록요청정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った年度の値を日雇い勤務一覧リクエスト情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setYear(int year) {
		this.year = year;
	}

	// 일용직근무목록요청정보 객체에 저장된 월 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 日雇い勤務一覧リクエスト情報オブジェクトに保存された月の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getMonth() {
		return month;
	}

	// 전달받은 월 값을 일용직근무목록요청정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った月の値を日雇い勤務一覧リクエスト情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setMonth(int month) {
		this.month = month;
	}

	// 일용직근무목록요청정보 객체에 저장된 Dept식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 日雇い勤務一覧リクエスト情報オブジェクトに保存されたDept識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Integer getDeptId() {
		return deptId;
	}

	// 전달받은 Dept식별번호 값을 일용직근무목록요청정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったDept識別番号の値を日雇い勤務一覧リクエスト情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setDeptId(Integer deptId) {
		this.deptId = deptId;
	}

	// 일용직근무목록요청정보 객체에 저장된 Pos식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 日雇い勤務一覧リクエスト情報オブジェクトに保存されたPos識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Integer getPosId() {
		return posId;
	}

	// 전달받은 Pos식별번호 값을 일용직근무목록요청정보 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったPos識別番号の値を日雇い勤務一覧リクエスト情報オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setPosId(Integer posId) {
		this.posId = posId;
	}
	

}
