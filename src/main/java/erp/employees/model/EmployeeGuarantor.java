package erp.employees.model;

import java.util.Date;

/**
 * [인사/사원관리] 사원 신원보증인 내역 Model DB 테이블: EMPLOYEE_GUARANTOR
 */
// 사원보증인 정보를 보관하고 관련 기능에서 사용할 수 있도록 제공한다.
// 社員保証人情報を保持し、関連機能から利用できるように提供する。
public class EmployeeGuarantor {

	// [DB 관리 항목]
	// データベースの主キー・外部キー・基本属性を保持し、永続化対象を識別する。
	private int employeeGuarantorId; // 보증인 식별 번호 (PK)
	private int employeeId; // 사원 식별 번호 (FK)

	// [보증인 입력 항목]
	// 画面で入力または照会する業務属性を保持し、登録・詳細表示で共通利用する。
	private String guarantorName; // 보증인 성명
	private String relation; // 관계
	private String juminNo; // 주민등록번호
	private Long guaranteeAmt; // 보증금액
	private Date guaranteeDate; // 보증일자(시작일)
	private Date expireDate; // 만료일자
	private String telNo; // 전화번호

	// 전달받은 값으로 사원보증인 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で社員保証人オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public EmployeeGuarantor() {
	}

	// Getter & Setter
	// 사원보증인 객체에 저장된 사원보증인식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された社員保証人識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getEmployeeGuarantorId() {
		return employeeGuarantorId;
	}

	// 전달받은 사원보증인식별번호 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った社員保証人識別番号の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setEmployeeGuarantorId(int employeeGuarantorId) {
		this.employeeGuarantorId = employeeGuarantorId;
	}

	// 사원보증인 객체에 저장된 사원식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された社員識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getEmployeeId() {
		return employeeId;
	}

	// 전달받은 사원식별번호 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った社員識別番号の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setEmployeeId(int employeeId) {
		this.employeeId = employeeId;
	}

	// 사원보증인 객체에 저장된 보증인명칭 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された保証人名称の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getGuarantorName() {
		return guarantorName;
	}

	// 전달받은 보증인명칭 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った保証人名称の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setGuarantorName(String guarantorName) {
		this.guarantorName = guarantorName;
	}

	// 사원보증인 객체에 저장된 관계 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された関係の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getRelation() {
		return relation;
	}

	// 전달받은 관계 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った関係の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setRelation(String relation) {
		this.relation = relation;
	}

	// 사원보증인 객체에 저장된 주민번호번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された住民番号番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getJuminNo() {
		return juminNo;
	}

	// 전달받은 주민번호번호 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った住民番号番号の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setJuminNo(String juminNo) {
		this.juminNo = juminNo;
	}

	// 사원보증인 객체에 저장된 보증금액 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された保証金額の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Long getGuaranteeAmt() {
		return guaranteeAmt;
	}

	// 전달받은 보증금액 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った保証金額の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setGuaranteeAmt(Long guaranteeAmt) {
		this.guaranteeAmt = guaranteeAmt;
	}

	// 사원보증인 객체에 저장된 보증일자 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存された保証日付の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Date getGuaranteeDate() {
		return guaranteeDate;
	}

	// 전달받은 보증일자 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った保証日付の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setGuaranteeDate(Date guaranteeDate) {
		this.guaranteeDate = guaranteeDate;
	}

	// 사원보증인 객체에 저장된 Expire일자 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存されたExpire日付の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public Date getExpireDate() {
		return expireDate;
	}

	// 전달받은 Expire일자 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったExpire日付の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setExpireDate(Date expireDate) {
		this.expireDate = expireDate;
	}

	// 사원보증인 객체에 저장된 Tel번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員保証人オブジェクトに保存されたTel番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getTelNo() {
		return telNo;
	}

	// 전달받은 Tel번호 값을 사원보증인 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったTel番号の値を社員保証人オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setTelNo(String telNo) {
		this.telNo = telNo;
	}
}
