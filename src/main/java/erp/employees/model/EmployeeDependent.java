package erp.employees.model;

/**
 * [인사/사원관리] 사원 부양가족 및 소득세 공제 설정 Model DB 테이블: EMPLOYEE_DEPENDENT
 */
// 사원부양가족 정보를 보관하고 관련 기능에서 사용할 수 있도록 제공한다.
// 社員扶養家族情報を保持し、関連機能から利用できるように提供する。
public class EmployeeDependent {

	// [DB 관리 항목]
	// データベースの主キー・外部キー・基本属性を保持し、永続化対象を識別する。
	private int employeeDependentId; // 부양가족 식별 번호 (PK)
	private int employeeId; // 사원 식별 번호 (FK)

	// [부양가족 기본정보]
	// 社員との続柄、氏名、生年月日など、扶養家族の基本情報を保持する。
	private String relation; // 관계
	private String depName; // 성명
	private String nationalType; // 구분 (내국인/외국인)
	private String juminNo; // 주민등록번호

	// [공제 적용 설정]
	// 支給額・控除額・期間値を業務基準に従って計算し、合計と最終金額へ反映する。
	private String disabledYn; // 장애여부
	private String basicDeductYn; // 인적공제 적용여부
	private String healthInsYn; // 건강보험 적용여부
	private String cohabitYn; // 동거여부
	private String incomeTaxYn; // 갑근세 적용여부
	private String childUnder20Yn; // 20세 이하 자녀 여부

	// 전달받은 값으로 사원부양가족 객체의 초기 상태를 구성한다.
	// 생성 시 전달된 필수값을 각 필드에 보관하여 이후 조회와 화면 출력에서 재사용한다.
	// 受け取った値で社員扶養家族オブジェクトの初期状態を構成する。
	// 生成時に受け取った必須値を各フィールドへ保持し、後続の照会と画面表示で再利用する。
	public EmployeeDependent() {
	}

	// Getter & Setter
	// 사원부양가족 객체에 저장된 사원부양가족식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された社員扶養家族識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getEmployeeDependentId() {
		return employeeDependentId;
	}

	// 전달받은 사원부양가족식별번호 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った社員扶養家族識別番号の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setEmployeeDependentId(int employeeDependentId) {
		this.employeeDependentId = employeeDependentId;
	}

	// 사원부양가족 객체에 저장된 사원식별번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された社員識別番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public int getEmployeeId() {
		return employeeId;
	}

	// 전달받은 사원식별번호 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った社員識別番号の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setEmployeeId(int employeeId) {
		this.employeeId = employeeId;
	}

	// 사원부양가족 객체에 저장된 관계 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された関係の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getRelation() {
		return relation;
	}

	// 전달받은 관계 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った関係の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setRelation(String relation) {
		this.relation = relation;
	}

	// 사원부양가족 객체에 저장된 Dep명칭 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存されたDep名称の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getDepName() {
		return depName;
	}

	// 전달받은 Dep명칭 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったDep名称の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setDepName(String depName) {
		this.depName = depName;
	}

	// 사원부양가족 객체에 저장된 국민구분 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された国民区分の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getNationalType() {
		return nationalType;
	}

	// 전달받은 국민구분 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った国民区分の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setNationalType(String nationalType) {
		this.nationalType = nationalType;
	}

	// 사원부양가족 객체에 저장된 주민번호번호 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された住民番号番号の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getJuminNo() {
		return juminNo;
	}

	// 전달받은 주민번호번호 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った住民番号番号の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setJuminNo(String juminNo) {
		this.juminNo = juminNo;
	}

	// 사원부양가족 객체에 저장된 장애인여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された障害者可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getDisabledYn() {
		return disabledYn;
	}

	// 전달받은 장애인여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った障害者可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setDisabledYn(String disabledYn) {
		this.disabledYn = disabledYn;
	}

	// 사원부양가족 객체에 저장된 Basic공제여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存されたBasic控除可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getBasicDeductYn() {
		return basicDeductYn;
	}

	// 전달받은 Basic공제여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取ったBasic控除可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setBasicDeductYn(String basicDeductYn) {
		this.basicDeductYn = basicDeductYn;
	}

	// 사원부양가족 객체에 저장된 건강보험여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された健康保険可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getHealthInsYn() {
		return healthInsYn;
	}

	// 전달받은 건강보험여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った健康保険可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setHealthInsYn(String healthInsYn) {
		this.healthInsYn = healthInsYn;
	}

	// 사원부양가족 객체에 저장된 동거여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された同居可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getCohabitYn() {
		return cohabitYn;
	}

	// 전달받은 동거여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った同居可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setCohabitYn(String cohabitYn) {
		this.cohabitYn = cohabitYn;
	}

	// 사원부양가족 객체에 저장된 소득세금여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された所得税金可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getIncomeTaxYn() {
		return incomeTaxYn;
	}

	// 전달받은 소득세금여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った所得税金可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setIncomeTaxYn(String incomeTaxYn) {
		this.incomeTaxYn = incomeTaxYn;
	}

	// 사원부양가족 객체에 저장된 자녀20세 이하여부 값을 반환한다.
	// 저장된 필드를 외부에 직접 노출하지 않고 접근 메서드를 통해 필요한 계층에 제공한다.
	// 社員扶養家族オブジェクトに保存された子供20歳以下可否の値を返す。
	// 保持しているフィールドを直接公開せず、アクセサーメソッドを通して必要な階層へ提供する。
	public String getChildUnder20Yn() {
		return childUnder20Yn;
	}

	// 전달받은 자녀20세 이하여부 값을 사원부양가족 객체에 저장한다.
	// 요청값이나 조회 결과로 전달된 값을 대응하는 필드에 반영하여 객체의 현재 상태를 갱신한다.
	// 受け取った子供20歳以下可否の値を社員扶養家族オブジェクトに保存する。
	// リクエスト値または照会結果として渡された値を対応フィールドへ反映し、オブジェクトの現在状態を更新する。
	public void setChildUnder20Yn(String childUnder20Yn) {
		this.childUnder20Yn = childUnder20Yn;
	}
}
